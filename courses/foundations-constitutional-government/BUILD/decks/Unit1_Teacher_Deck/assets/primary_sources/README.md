# Unit 1 primary-source images — drop zone

Drop the public-domain image files here using the exact filenames in
`../../unit1_images.json` (e.g. `GC.03_join-or-die.jpg`). The deck build
(`build_deck.py`) embeds any file it finds and otherwise renders a cited
placeholder frame. Source from the repository named in the manifest
(National Archives / Library of Congress / Smithsonian). Public domain only.
